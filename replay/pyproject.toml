[project]
name = "ticksense-replay"
version = "0.1.0"
description = "Kafka offset replay producer for historical backfill and incident recovery"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.11.0",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "structlog>=24.2",
    "pyiceberg[gcs,s3fs]>=0.7.0",
]
